fastapi==0.115.0
uvicorn[standard]==0.30.6
SQLAlchemy==2.0.35
psycopg2-binary==2.9.9
# Jinja2, python-multipart e python-dotenv: versão elevada pra corrigir
# falhas de segurança conhecidas (achadas pelo pip-audit — ver
# .github/workflows/security.yml), compatível sem mudança de código.
Jinja2==3.1.6
python-multipart==0.0.20
passlib==1.7.4
bcrypt==4.0.1
itsdangerous==2.2.0
python-dotenv==1.2.2
httpx==0.27.2
# Pillow atualizado pra versão mais recente (corrige as CVEs que o
# pip-audit apontava) — testado manualmente contra app/avatars.py
# (upload -> verify -> exif_transpose -> convert -> thumbnail -> save
# como WEBP) e contra a suíte de testes inteira, sem quebrar nada.
Pillow==12.3.0
# starlette segue "presa" à versão que o fastapi==0.115.0 traz junto —
# CVEs dela pendentes de revisão (exigiria subir o fastapi também).

# Painel financeiro interno (Zona Vermelha — ver app/routers/financial_routes.py):
# geração dos exports de fechamento mensal/anual.
openpyxl==3.1.5   # exportar .xlsx
reportlab==4.2.5  # exportar .pdf (puro Python, sem dependência de sistema tipo cairo/pango)
